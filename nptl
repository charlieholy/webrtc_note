#include <stdio.h>
#include <unistd.h>

int main() {
    char buf[256];
    confstr(_CS_GNU_LIBPTHREAD_VERSION, buf, sizeof(buf));
    printf("%s\n", buf);
    return 0;
}

ubuntu22@NYX:~/test$ ./a.out
NPTL 2.35

root@ATK-DLRK3568:/# getconf GNU_LIBPTHREAD_VERSION
NPTL 2.35
